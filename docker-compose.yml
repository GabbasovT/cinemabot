version: '3.9'

services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data

  bot:
    build: .
    restart: always
    env_file: .env
    depends_on:
      - db

volumes:
  pgdata:
